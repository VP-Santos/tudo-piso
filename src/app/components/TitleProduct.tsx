import { Box, Typography } from "@mui/material";

export default function TitleProduct() {
    return (
        <Box bgcolor={'white'} display={'flex'} mt={6} height={100}>
            img
            <Typography>Pisos Laminados</Typography>
        </Box>

    )
}